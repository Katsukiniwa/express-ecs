datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model users {
  id Int @default(autoincrement()) @id
}

model user_accounts {
  id Int @default(autoincrement()) @id
}